import { RouteInformation } from './route-information.model';

describe('RouteInformation', () => {
  it('should create an instance', () => {
    expect(new RouteInformation()).toBeTruthy();
  });
});
